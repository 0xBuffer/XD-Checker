<Page x:Class="XDCheckerRecode.ProgramsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:XDCheckerRecode"
      Title="ProgramsPage">
    <Page.Resources>
        <Style x:Key="ProgramCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2AFFFFFF"/>
                    <Setter Property="BorderBrush" Value="#40FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LaunchButtonStyle" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#007acc" Offset="0"/>
                        <GradientStop Color="#0056b3" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#33FFFFFF"/>
                    <Setter Property="Foreground" Value="#80FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20" ScrollViewer.CanContentScroll="False">
        <ItemsControl x:Name="ProgramsItemsControl">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource ProgramCardStyle}" Width="220" Height="280" Margin="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="100"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Icon Area with placeholder fallback -->
                            <Grid Grid.Row="0" Margin="10">
                                <Border Background="#10FFFFFF" CornerRadius="8"/>
                                <Image Source="{Binding IconUrl, IsAsync=True}" Stretch="Uniform" Margin="15" RenderOptions.BitmapScalingMode="HighQuality"/>
                                <!-- Fallback if no icon (logic in style triggers could be added but simplify for now) -->
                            </Grid>

                            <!-- Text Content -->
                            <StackPanel Grid.Row="1" Margin="15,0,15,5">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" Foreground="White" 
                                           TextAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="80" Margin="0,5,0,0">
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#B0B0B0" 
                                               TextWrapping="Wrap" TextAlignment="Center"/>
                                </ScrollViewer>
                            </StackPanel>

                            <!-- Action Button -->
                            <Button Grid.Row="2" Margin="15,0,15,15" 
                                    Style="{StaticResource LaunchButtonStyle}" Click="LaunchButton_Click"
                                    IsEnabled="{Binding IsAvailable}">
                                <Button.Content>
                                    <Binding Path="IsAvailable">
                                        <Binding.Converter>
                                            <local:AvailabilityToTextConverter/>
                                        </Binding.Converter>
                                    </Binding>
                                </Button.Content>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>
